@{
    ViewData["Title"] = "List Education";
}

<section class="section">
    <div class="section-header">
        <h1>Education</h1>
    </div>

    <div class="section-body">
        <div class="row">
            <div class="col-12 col-md-12 col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4>List Education</h4>
                        <div class="card-header-action">
                            <button type="button" class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addModal">Add Education</button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped" id="education-table">
                                <thead>
                                    <tr>
                                        <th scope="col">#</th>
                                        <th scope="col">Major</th>
                                        <th scope="col">Degree</th>
                                        <th scope="col">GPA</th>
                                        <th scope="col">Aksi</th>
                                    </tr>
                                </thead>
                                <tbody id="education-list">
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="~/js/education.js"></script>
}
